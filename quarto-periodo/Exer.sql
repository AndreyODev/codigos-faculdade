CREATE TABLE alunos(
	id_aluno INTEGER PRIMARY KEY,
	nome varchar(100),
	matricula varchar(10),
	email varchar(100),
	curso_id INTEGER,
	FOREIGN KEY (id_curso) REFERENCES cursos_disciplinas(id_curso)
	
);

CREATE TABLE cursos_disciplinas(
	id_curso INTEGER PRIMARY KEY,
	nome_curso VARCHAR(100),
	codigo VARCHAR(10),
	disciplina VARCHAR(100),
	periodo VARCHAR(20),
	professor_id INTEGER,
	FOREIGN KEY (professor_id) REFERENCES professores(id_professores)
);

INSERT INTO cursos_disciplinas (id_curso, nome_curso, codigo, disciplina, periodo, professor_id) 
VALUES(1, 'Engenharia de Software', '1qaz', 'Algoritmo', '1° Período', 1);

INSERT INTO cursos_disciplinas (id_curso, nome_curso, codigo, disciplina, periodo, professor_id) 
VALUES(1, 'Assistente Administrati', '1qaz', 'Algoritmo', '1° Período', 1);


SELECT * FROM cursos_disciplinas;

CREATE TABLE professores(
	id_professores INTEGER PRIMARY KEY,
	nome VARCHAR(100),
	email VARCHAR(100),
	departamento VARCHAR(100)
);

INSERT INTO professores (id_professores, nome, email, departamento) 
VALUES (1, 'Andrey', 'andrey@gmail.com', 'Acadêmico');

SELECT * FROM professores;


































































































































































































































































































































































































































































































































































































	
)
